import 'package:flutter/material.dart';

class Widgets {
  Widget connectionerror() {
    return Scaffold(
      body: Center(
        child: Text('No internet connection Found'),
      ),
    );
  }
}
